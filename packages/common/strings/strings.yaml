## translation, entry file.
#title: Welcome to Flutter Translation Sheet tool

# body: |
#   This is a sample body
#   to check the translation system
#   in GoogleSheet

## you can reference other files and folders with [$ref: path].
## content of the file will be unwrapped into the key.
home:
  $ref: home.yaml
sales:
  $ref: sales.yaml
merchandiser:
  $ref: merchandiser.yaml
orderHistory:
  $ref: orderHistory.yaml
setting:
  $ref: setting.yaml
profile:
  $ref: profile.yaml
report:
  $ref: report.yaml
product:
  $ref: product.yaml
common:
  $ref: common.yaml

